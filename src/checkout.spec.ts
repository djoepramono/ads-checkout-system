// import { Checkout } from './checkout';

describe('checkout', () => {

  it('has undefined state until built', async () => {
    // const customer = { name: 'John Doe' };
    // const pricingRule =
    expect(true).toStrictEqual(true);
  });
});